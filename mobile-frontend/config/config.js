// Optional Hugging Face token — do not commit real tokens.
// Gemma inference runs on the FastAPI backend; this file is usually unused.
export const HUGGINGFACE_API_KEY = '';

// AssemblyAI: use `EXPO_PUBLIC_ASSEMBLYAI_API_KEY` in `.env` — see `config/assemblyaiConfig.ts`.
